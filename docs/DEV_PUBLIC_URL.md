# Public dev URL (ngrok)

Sometimes you want links that look like a real SaaS URL instead of `localhost`.

## Option A: ngrok (recommended for quick demos)

1. Install ngrok
2. Run your frontend locally:

```bash
cd frontend
npm run dev
```
